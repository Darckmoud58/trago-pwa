# TraGo — variables (monorepo Vite + Express)

# server/.env
# PORT=4000
# MONGODB_URI=mongodb://127.0.0.1:27017/trago
# JWT_SECRET=cambia-esto

# trago_web/.env  (igual que Todo_pwa: VITE_API_URL)
# VITE_API_URL=http://localhost:4000
