# trago_web

Front TraGo — Vite + React + TypeScript + axios (mismo patrón que `Todo_pwa/todo_pwaa`).

```bash
cp .env.example .env   # VITE_API_URL=http://localhost:4000
npm install
npm run dev
```

API: `../server` → `npm start`
